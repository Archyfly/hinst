<h3>Post for current user</h3>

<h4><%= @post.title %></h4>
<div><%= @post.body %></div>
<br/>

Comments:
<% @post.comments.each do |comment| %>
  <div><%= comment.description %></div>
<% end %>
=========================
<h4>Other posts this user</h4>
<div><% @user_posts.each do |post| %>
    post_id: <%= post.id %>
    <%= link_to "#{post.title}", user_post_path(post.user_id, post.id) %>
    <% end %>
</div>



